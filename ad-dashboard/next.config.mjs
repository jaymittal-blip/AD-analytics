/** @type {import('next').NextConfig} */
const nextConfig = {
  // When integrating with the Node.js backend, add the backend domain here
  // e.g. images: { domains: ['your-backend.com'] }
};

export default nextConfig;
